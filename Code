public class Multiples {
    public static int[] find(int base, int limit) {
     
      int[] newArray = new int[limit/base];
      int count = 0;
     
      for(int i = base; i<=limit; i+=base){
        newArray[count] = i;
        count++;

      }

        return newArray;
    }
}
